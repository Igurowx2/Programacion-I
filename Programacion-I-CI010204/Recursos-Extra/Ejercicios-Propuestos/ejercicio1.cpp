/*1. Desarrollar un programa que simule un sistema Bancario, que lance una excepcion,
si se supera el saldo a retirar

2. Crear una clase Estudiante que contenga un atributo promedio y que genere una excepción si la nota
esta fuera de rango [0, 20] 

3. Crear un programa que lea un archivo txt y que maneje excepciones si el archivo no existe o no se puede abrir */

git init
git add main.cpp
git commit -m "Sistema de alquiler de vehículos AutoRenta Perú"
git remote add origin https://github.com/tu_usuario/AutoRentaCPP.git
git push -u origin main